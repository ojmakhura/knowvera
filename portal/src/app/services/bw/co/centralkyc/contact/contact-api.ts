// Generated by andromda-angular cartridge (service\service.impl.ts.vsl) CAN EDIT
import { Injectable, inject } from '@angular/core';
import { Observable } from 'rxjs';
import { ContactDTO } from '@models/bw/co/centralkyc/contact/contact-dto';
import { HttpClient } from '@angular/common/http';
import { Page } from '@models/page.model';
import { SearchObject } from '@models/search-object';
import { ContactType } from '@app/models/bw/co/centralkyc/contact/contact-type';

@Injectable({
  providedIn: 'root'
})
export class ContactApi {
    
    protected path = '/contact';

    private http = inject(HttpClient);

    public findById(id: string): Observable<ContactDTO> {

        return this.http.get<ContactDTO>(`${this.path}/${id}`);
    }

    public findByType(type: ContactType): Observable<ContactDTO[]> {

        return this.http.get<ContactDTO[]>(`${this.path}/by-type/${type}`);
    }

    public findByTypePaged(type: ContactType, pageNumber: number, pageSize: number): Observable<Page<ContactDTO>> {

        return this.http.get<Page<ContactDTO>>(`${this.path}/by-type/${type}/paged?pageNumber=${pageNumber}&pageSize=${pageSize}`);
    }

    public getAll(): Observable<ContactDTO[]> {

        return this.http.get<ContactDTO[]>(`${this.path}`);
    }

    public getAllPaged(pageNumber: number, pageSize: number): Observable<Page<ContactDTO>> {

        return this.http.get<Page<ContactDTO>>(`${this.path}/paged?pageNumber=${pageNumber}&pageSize=${pageSize}`);
    }

    public remove(id: string): Observable<boolean> {

        return this.http.delete<boolean>(`${this.path}/${id}`);
    }

    public save(document: ContactDTO): Observable<ContactDTO> {

        return this.http.post<ContactDTO>(`${this.path}`, document);
    }

    public search(criteria: string): Observable<ContactDTO[]> {

        return this.http.get<ContactDTO[]>(`${this.path}/search?criteria=${criteria}`);
    }

}
