// Generated by andromda-angular cartridge (view\view.component.imp.ts.vsl) CAN EDIT!
import { Component, effect, linkedSignal } from '@angular/core';
import { SearchIndividualsComponent } from '@views/individual/search/search-individuals';
import { CommonModule } from '@angular/common';
import { FormsModule, ReactiveFormsModule } from '@angular/forms';
import { TranslateModule } from '@ngx-translate/core';
import { MaterialModule } from '@app/material.module';
import { TableComponent } from '@components/table/table';
import { Loader } from "@shared/loader/loader";
import { FormField } from '@angular/forms/signals';
import { MatPaginator } from '@angular/material/paginator';
import { IndividualSearchCriteria } from '@app/models/bw/co/centralkyc/individual/individual-search-criteria';
import { SearchObject } from '@app/models/search-object';

@Component({
  selector: 'app-search-individuals',
  templateUrl: './search-individuals.html',
  styleUrls: ['./search-individuals.scss'],
  standalone: true,
  imports: [
    CommonModule,
    TranslateModule,
    MaterialModule,
    TableComponent,
    Loader,
    FormField,
  ],
})
export class SearchIndividualsImplComponent extends SearchIndividualsComponent {
  override error = linkedSignal(() => this.individualApiStore.error());
  override messages = linkedSignal(() => this.individualApiStore.messages());
  override success = linkedSignal(() => this.individualApiStore.success());
  override loading = linkedSignal(() => this.individualApiStore.loading());

  override individualsTableSignal = linkedSignal(() => this.individualApiStore.dataPage());

  constructor() {
    super();

  }
  doNgOnDestroy(): void {
  }

  override doNgAfterViewInit(): void {
    this.individualsTable?.tablePaginator?.page?.subscribe({
      next: (paginator: MatPaginator) => {
        this.doSearch(paginator.pageIndex, paginator.pageSize);
      },
    });

  }

  override beforeSearchIndividualsSearch(form: any): void {
    this.doSearch();
  }

  private doSearch(pageNumber: number = 0, pageSize: number = 10): void {
    let value = this.searchIndividualsSignal();

    let criteria = new SearchObject<IndividualSearchCriteria>()
    criteria.pageNumber = pageNumber;
    criteria.pageSize = pageSize;
    criteria.criteria = value;

    this.individualApiStore.pagedSearch({
      criteria: criteria
    });
  }
}
