// license-header java merge-point
//
// Attention: Generated code! Do not modify by hand!
// Generated by SpringWSImpl.java.vsl in andromda-webservices.
//
package bw.co.centralkyc.kyc.verification;

import bw.co.centralkyc.AuditTracker;
import bw.co.centralkyc.SearchObject;
import bw.co.centralkyc.kyc.KycRecordSearchCriteria;
import io.swagger.v3.oas.annotations.Operation;
import io.swagger.v3.oas.annotations.tags.Tag;
import java.util.Collection;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.data.domain.Page;
import org.springframework.http.ResponseEntity;
import org.springframework.security.core.Authentication;
import org.springframework.security.core.context.SecurityContextHolder;
import org.springframework.web.bind.annotation.RestController;

@RestController
@Tag(name = "KYC Record Verification", description = "Verification of KYC records.")
public class KycVerificationApiImpl implements KycVerificationApi {

    private static final Logger logger = LoggerFactory.getLogger(KycVerificationApiImpl.class);
        protected final KycVerificationService kycVerificationService;
    
    public KycVerificationApiImpl(
        KycVerificationService kycVerificationService    ) {
        
        this.kycVerificationService = kycVerificationService;
    }



    @Override
    @Operation(summary = "Find Verification", description = "Get the kyc verification with the given id")
    public ResponseEntity<KycVerificationDTO> findById(String id) throws Exception {
        try {
            return ResponseEntity.ok(kycVerificationService.findById(id));
        } catch (Exception e) {

            logger.error("An error occurred while processing the request", e);
            throw e.getCause() != null ? new Exception(e.getCause()) : e;
        } 
    }


    @Override
    @Operation(summary = "Find By Record", description = "Get the verification by the record id.")
    public ResponseEntity<KycVerificationDTO> findByRecord(String recordId) throws Exception {
        try {
            return ResponseEntity.ok(kycVerificationService.findByRecord(recordId));
        } catch (Exception e) {

            logger.error("An error occurred while processing the request", e);
            throw e.getCause() != null ? new Exception(e.getCause()) : e;
        } 
    }


    @Override
    public ResponseEntity<Collection<KycVerificationDTO>> getAll() throws Exception {
        try {
            return ResponseEntity.ok(kycVerificationService.getAll());
        } catch (Exception e) {

            logger.error("An error occurred while processing the request", e);
            throw e.getCause() != null ? new Exception(e.getCause()) : e;
        } 
    }


    @Override
    public ResponseEntity<Page<KycVerificationDTO>> getAllPaged(Integer pageNumber, Integer pageSize) throws Exception {
        try {
            return ResponseEntity.ok(kycVerificationService.getAll(pageNumber, pageSize));
        } catch (Exception e) {

            logger.error("An error occurred while processing the request", e);
            throw e.getCause() != null ? new Exception(e.getCause()) : e;
        } 
    }


    @Override
    public ResponseEntity<Page<KycVerificationDTO>> pagedSearch(SearchObject<KycRecordSearchCriteria> criteria) throws Exception {
        try {
            return ResponseEntity.ok(kycVerificationService.search(criteria));
        } catch (Exception e) {

            logger.error("An error occurred while processing the request", e);
            throw e.getCause() != null ? new Exception(e.getCause()) : e;
        } 
    }


    @Override
    public ResponseEntity<Boolean> remove(String id) throws Exception {
        try {
            return ResponseEntity.ok(kycVerificationService.remove(id));
        } catch (Exception e) {

            logger.error("An error occurred while processing the request", e);
            throw e.getCause() != null ? new Exception(e.getCause()) : e;
        } 
    }


    @Override
    public ResponseEntity<KycVerificationDTO> save(KycVerificationDTO kycVerification) throws Exception {
        try {
            Authentication authentication = SecurityContextHolder.getContext().getAuthentication();
            AuditTracker.auditTrail(kycVerification, authentication);
            return ResponseEntity.ok(kycVerificationService.save(kycVerification));
        } catch (Exception e) {

            logger.error("An error occurred while processing the request", e);
            throw e.getCause() != null ? new Exception(e.getCause()) : e;
        } 
    }


    @Override
    public ResponseEntity<Collection<KycVerificationDTO>> search(KycVerificationSearchCriteria criteria) throws Exception {
        try {
            return ResponseEntity.ok(kycVerificationService.search(criteria));
        } catch (Exception e) {

            logger.error("An error occurred while processing the request", e);
            throw e.getCause() != null ? new Exception(e.getCause()) : e;
        } 
    }
    
    /**
     * Gets the reference to <code>$serviceRef.daoName</code>.
     * @return kycVerificationService
     */
    protected KycVerificationService getKycVerificationService()
    {
        return this.kycVerificationService;
    }
}