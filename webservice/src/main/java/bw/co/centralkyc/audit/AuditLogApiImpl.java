// license-header java merge-point
//
// Attention: Partially Generated code! Can be modified by hand!
// Generated by SpringWSImpl.java.vsl in andromda-webservices.
//
package bw.co.centralkyc.audit;

import io.swagger.v3.oas.annotations.Operation;
import io.swagger.v3.oas.annotations.tags.Tag;
import jakarta.validation.Valid;
import java.util.List;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.data.domain.Page;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.RestController;

@RestController
@Tag(name = "Document Type", description = "Managing document types.")
public class AuditLogApiImpl implements AuditLogApi {

    private static final Logger logger = LoggerFactory.getLogger(AuditLogApiImpl.class);
        protected final AuditLogService auditLogService;
    
    public AuditLogApiImpl(
        AuditLogService auditLogService    ) {
        
        this.auditLogService = auditLogService;
    }

    @Override
    @Operation(summary = "Find Audit Log", description = "Get the audit log with the given id")
    public ResponseEntity<AuditLogDTO> findById(String id) throws Exception {
        try {
            return ResponseEntity.ok(auditLogService.findById(id));
        } catch (Exception e) {

            logger.error("An error occurred while processing the request", e);
            throw e.getCause() != null ? new AuditLogServiceException(e.getCause().getMessage()) : e;
        } 
    }

    @Override
    public ResponseEntity<List<AuditLogDTO>> getAll() throws Exception {
        try {
            return ResponseEntity.ok(auditLogService.getAll());
        } catch (Exception e) {

            logger.error("An error occurred while processing the request", e);
            throw e.getCause() != null ? new AuditLogServiceException(e.getCause().getMessage()) : e;
        } 
    }

    @Override
    public ResponseEntity<Page<AuditLogDTO>> getAllPaged(Integer pageNumber, Integer pageSize) throws Exception {
        try {
            return ResponseEntity.ok(auditLogService.getAll(pageNumber, pageSize));
        } catch (Exception e) {

            logger.error("An error occurred while processing the request", e);
            throw e.getCause() != null ? new AuditLogServiceException(e.getCause().getMessage()) : e;
        } 
    }


    @Override
    public ResponseEntity<Page<AuditLogDTO>> pagedSearch(@Valid AuditLogCriteria criteria, Integer pageNumber, Integer pageSize) throws Exception {
        try {
            return ResponseEntity.ok(auditLogService.search(criteria, pageNumber, pageSize));
        } catch (Exception e) {

            logger.error("An error occurred while processing the request", e);
            throw e.getCause() != null ? new AuditLogServiceException(e.getCause().getMessage()) : e;
        } 
    }


    @Override
    public ResponseEntity<Boolean> remove(String id) throws Exception {
        try {
            return ResponseEntity.ok(auditLogService.remove(id));
        } catch (Exception e) {

            logger.error("An error occurred while processing the request", e);
            throw e.getCause() != null ? new AuditLogServiceException(e.getCause().getMessage()) : e;
        } 
    }


    @Override
    public ResponseEntity<AuditLogDTO> save(@Valid AuditLogDTO auditLog) throws Exception {
        try {
            return ResponseEntity.ok(auditLogService.save(auditLog));
        } catch (Exception e) {

            logger.error("An error occurred while processing the request", e);
            throw e.getCause() != null ? new AuditLogServiceException(e.getCause().getMessage()) : e;
        } 
    }


    @Override
    public ResponseEntity<List<AuditLogDTO>> search(@Valid AuditLogCriteria criteria) throws Exception {
        try {
            return ResponseEntity.ok(auditLogService.search(criteria));
        } catch (Exception e) {

            logger.error("An error occurred while processing the request", e);
            throw e.getCause() != null ? new AuditLogServiceException(e.getCause().getMessage()) : e;
        } 
    }
    
    /**
     * Gets the reference to <code>$serviceRef.daoName</code>.
     * @return auditLogService
     */
    protected AuditLogService getAuditLogService()
    {
        return this.auditLogService;
    }
}