// license-header java merge-point
//
// Attention: Generated code! Do not modify by hand!
// Generated by controller-advice.java.vsl in andromda-spring-cartridge. Do not modify by hand!.
//
package bw.co.knowvera.organisation.client;

import bw.co.knowvera.ErrorResponse;
import java.time.Instant;
import java.util.Arrays;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.core.Ordered;
import org.springframework.core.annotation.Order;
import org.springframework.http.HttpStatus;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.ExceptionHandler;
import org.springframework.web.bind.annotation.RestControllerAdvice;

/**
 * Translates the errors thrown by {@link ClientRequestApiServiceBase}
 * into human-readable API responses for the clientRequestApi endpoints.
 *
 * Scoped to {@link ClientRequestApi} only, and ordered ahead
 * of global exception handlers so that this advice's friendlier messages take precedence.
 */
@RestControllerAdvice(assignableTypes = ClientRequestApi.class)
@Order(Ordered.HIGHEST_PRECEDENCE)
public class ClientRequestApiControllerAdvice {

    private static final Logger LOGGER = LoggerFactory.getLogger(ClientRequestApiControllerAdvice.class);

    /**
     * Handles manual argument validation checks thrown by ClientRequestApiServiceBase.
     * Maps generated validation messages to friendly human-readable descriptions.
     */
    @ExceptionHandler(IllegalArgumentException.class)
    public ResponseEntity<ErrorResponse> handleIllegalArgument(IllegalArgumentException ex) {
        String message = ex.getMessage() == null ? "" : ex.getMessage();
        String friendly = toFriendlyMessage(message);

        LOGGER.warn("ClientRequestApi validation failed: {}", message);

        ErrorResponse errorResponse = new ErrorResponse(
                HttpStatus.BAD_REQUEST.value(),
                HttpStatus.BAD_REQUEST.name(),
                friendly,
                Arrays.asList(ex.getMessage()),
                Instant.now()
        );

        return ResponseEntity.status(HttpStatus.BAD_REQUEST)
                .body(errorResponse);
    }

    /**
     * Handles {@link ClientRequestServiceException} thrown by referenced services.
     */
    @ExceptionHandler(ClientRequestServiceException.class)
    public ResponseEntity<ErrorResponse> handleClientRequestServiceException(ClientRequestServiceException ex) {
        LOGGER.error("Referenced service error (ClientRequestService)", ex);

        return ResponseEntity.status(HttpStatus.INTERNAL_SERVER_ERROR)
                .body(new ErrorResponse(
                        HttpStatus.INTERNAL_SERVER_ERROR.value(),
                        HttpStatus.INTERNAL_SERVER_ERROR.name(),
                        "An error occurred while communicating with dependent services. Please try again shortly.",
                        null,
                        Instant.now()
                ));
    }

    /**
     * Maps validation messages thrown by ClientRequestApiServiceBase to plain-language equivalents.
     */
    private String toFriendlyMessage(String message) {
        if (message.contains(".findById(") && message.contains("'id'")) {
            return "Id is required.";
        }

        if (message.contains(".save(") && message.contains("'clientRequest'")) {
            return "Client Request is required.";
        }

        if (message.contains(".remove(") && message.contains("'id'")) {
            return "Id is required.";
        }

        if (message.contains(".findByOrganisation(") && message.contains("'organisationId'")) {
            return "Organisation Id is required.";
        }

        if (message.contains(".findByOrganisationPaged(") && message.contains("'organisationId'")) {
            return "Organisation Id is required.";
        }

        if (message.contains(".findByStatus(") && message.contains("'status'")) {
            return "Status is required.";
        }

        if (message.contains(".findByIndividual(") && message.contains("'individualId'")) {
            return "Individual Id is required.";
        }

        if (message.contains(".findByIndividualPaged(") && message.contains("'individualId'")) {
            return "Individual Id is required.";
        }

        if (message.contains(".findByDocument(") && message.contains("'documentId'")) {
            return "Document Id is required.";
        }

        if (message.contains(".findByDocumentPaged(") && message.contains("'documentId'")) {
            return "Document Id is required.";
        }

        if (message.contains(".findByStatusPaged(") && message.contains("'status'")) {
            return "Status is required.";
        }

        if (message.contains(".uploadRequests(") && message.contains("'file'")) {
            return "File is required.";
        }

        if (message.contains(".findByTarget(") && message.contains("'target'")) {
            return "Target is required.";
        }

        if (message.contains(".findByTarget(") && message.contains("'targetId'")) {
            return "Target Id is required.";
        }

        if (message.contains(".findByTargetPaged(") && message.contains("'target'")) {
            return "Target is required.";
        }

        if (message.contains(".findByTargetPaged(") && message.contains("'targetId'")) {
            return "Target Id is required.";
        }

        if (message.contains(".findOrganisationsByOrganisation(") && message.contains("'organisationId'")) {
            return "Organisation Id is required.";
        }

        if (message.contains(".findIndividualsByOrganisation(") && message.contains("'organisationId'")) {
            return "Organisation Id is required.";
        }

        if (message.contains(".findOrganisationsByOrganisationPaged(") && message.contains("'organisationId'")) {
            return "Organisation Id is required.";
        }

        if (message.contains(".findIndividualsByOrganisationPaged(") && message.contains("'organisationId'")) {
            return "Organisation Id is required.";
        }

        if (message.contains(".updateStatus(") && message.contains("'id'")) {
            return "Id is required.";
        }

        if (message.contains(".updateStatus(") && message.contains("'status'")) {
            return "Status is required.";
        }

        if (message.contains(".confirmToken(") && message.contains("'requestId'")) {
            return "Request Id is required.";
        }

        if (message.contains(".confirmToken(") && message.contains("'token'")) {
            return "Token is required.";
        }

        if (message.contains(".confirmRegistration(") && message.contains("'id'")) {
            return "Id is required.";
        }

        if (message.contains(".confirmRegistration(") && message.contains("'registrationToken'")) {
            return "Registration Token is required.";
        }


        // Fallback for any unmapped validation message
        return message;
    }
}