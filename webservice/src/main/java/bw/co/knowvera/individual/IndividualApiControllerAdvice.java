// license-header java merge-point
//
// Attention: Generated code! Do not modify by hand!
// Generated by controller-advice.java.vsl in andromda-spring-cartridge. Do not modify by hand!.
//
package bw.co.knowvera.individual;

import bw.co.knowvera.ErrorResponse;
import java.time.Instant;
import java.util.Arrays;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.core.Ordered;
import org.springframework.core.annotation.Order;
import org.springframework.http.HttpStatus;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.ExceptionHandler;
import org.springframework.web.bind.annotation.RestControllerAdvice;

/**
 * Translates the errors thrown by {@link IndividualApiServiceBase}
 * into human-readable API responses for the individualApi endpoints.
 *
 * Scoped to {@link IndividualApi} only, and ordered ahead
 * of global exception handlers so that this advice's friendlier messages take precedence.
 */
@RestControllerAdvice(assignableTypes = IndividualApi.class)
@Order(Ordered.HIGHEST_PRECEDENCE)
public class IndividualApiControllerAdvice {

    private static final Logger LOGGER = LoggerFactory.getLogger(IndividualApiControllerAdvice.class);

    /**
     * Handles manual argument validation checks thrown by IndividualApiServiceBase.
     * Maps generated validation messages to friendly human-readable descriptions.
     */
    @ExceptionHandler(IllegalArgumentException.class)
    public ResponseEntity<ErrorResponse> handleIllegalArgument(IllegalArgumentException ex) {
        String message = ex.getMessage() == null ? "" : ex.getMessage();
        String friendly = toFriendlyMessage(message);

        LOGGER.warn("IndividualApi validation failed: {}", message);

        ErrorResponse errorResponse = new ErrorResponse(
                HttpStatus.BAD_REQUEST.value(),
                HttpStatus.BAD_REQUEST.name(),
                friendly,
                Arrays.asList(ex.getMessage()),
                Instant.now()
        );

        return ResponseEntity.status(HttpStatus.BAD_REQUEST)
                .body(errorResponse);
    }

    /**
     * Handles {@link IndividualServiceException} thrown by referenced services.
     */
    @ExceptionHandler(IndividualServiceException.class)
    public ResponseEntity<ErrorResponse> handleIndividualServiceException(IndividualServiceException ex) {
        LOGGER.error("Referenced service error (IndividualService)", ex);

        return ResponseEntity.status(HttpStatus.INTERNAL_SERVER_ERROR)
                .body(new ErrorResponse(
                        HttpStatus.INTERNAL_SERVER_ERROR.value(),
                        HttpStatus.INTERNAL_SERVER_ERROR.name(),
                        "An error occurred while communicating with dependent services. Please try again shortly.",
                        null,
                        Instant.now()
                ));
    }

    /**
     * Maps validation messages thrown by IndividualApiServiceBase to plain-language equivalents.
     */
    private String toFriendlyMessage(String message) {
        if (message.contains(".findById(") && message.contains("'id'")) {
            return "Id is required.";
        }

        if (message.contains(".save(") && message.contains("'individual'")) {
            return "Individual is required.";
        }

        if (message.contains(".remove(") && message.contains("'id'")) {
            return "Id is required.";
        }

        if (message.contains(".pagedSearch(") && message.contains("'criteria'")) {
            return "Criteria is required.";
        }

        if (message.contains(".getOrganisationClientsPaged(") && message.contains("'organisationId'")) {
            return "Organisation Id is required.";
        }

        if (message.contains(".loadRequestIndividual(") && message.contains("'requestId'")) {
            return "Request Id is required.";
        }

        if (message.contains(".loadRequestIndividual(") && message.contains("'identityConfirmationToken'")) {
            return "Identity Confirmation Token is required.";
        }

        if (message.contains(".loadRequestIndividual(") && message.contains("'identityNo'")) {
            return "Identity No is required.";
        }

        if (message.contains(".verifyIndividual(") && message.contains("'id'")) {
            return "Id is required.";
        }


        // Fallback for any unmapped validation message
        return message;
    }
}